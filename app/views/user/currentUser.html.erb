<%if @currentUserPass == true %>	
Mis mascotas<br>
	
	<% if user_signed_in? %>
		<%= @currentUser.email %> 
	<%end%>
	<br>
	
	<% if admin_signed_in? %>
		<%= @currentUser.name %> 
	<%end%><br>
	
	
	<% if protectora_signed_in? %>
		<%= @currentUser.nombre %> 
	<%end%>
	
	

	<% @misMascotas.each do |miMascota| %>
	<%= link_to mascotum_path(miMascota) do %>
	<%= miMascota.nombre%>
	<%end%>
	</a>
	<%= miMascota.status	%> <td><%= link_to 'Edit', edit_mascotum_path(miMascota) %></td>
	<td><%= miMascota.formularios.count%> </td>
    <br>
	<%end%>


<% else %>
Upa upa a dónde?
<%end%>